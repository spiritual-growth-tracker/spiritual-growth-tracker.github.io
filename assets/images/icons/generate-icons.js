const sharp=require("sharp"),fs=require("fs"),path=require("path");async function generateIcons(){const e=[16,32,48,64,128,192,256,512],n=path.join(__dirname,"app-icon.svg"),r=__dirname;for(const o of e)await sharp(n).resize(o,o).png().toFile(path.join(r,`icon-${o}x${o}.png`));const o=await Promise.all([16,32,48].map((e=>sharp(n).resize(e,e).png().toBuffer()))),a=require("ico-endec").encode(o);fs.writeFileSync(path.join(r,"favicon.ico"),a)}generateIcons().catch(console.error);